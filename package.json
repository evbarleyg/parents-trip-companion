{
  "name": "parents-trip-companion",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "dev:api": "npm run dev --workspace api",
    "lint": "npm run lint --workspace web && npm run lint --workspace api",
    "test": "npm run test --workspace web && npm run test --workspace api",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace api",
    "build": "npm run build --workspace web && npm run build --workspace api"
  }
}
